/**
 * Phase 8 — shared baseline-ML constants.
 *
 * The at-risk threshold matches `src/lib/confidence-label.ts` so the
 * prediction layer and the riskMeta UI agree on what "at-risk" means.
 * If you change one, change the other.
 */

export const AT_RISK_THRESHOLD = 55;
